#Configurable properties

{
  login.message = ""

  # The string "env" gets replaced with actual environment name
  appUrl = "https://env-url.com/"
  adminUrl = "https://env-adminurl.com/"
  apiUrl = "https://env-url/api"

  # Expected values are "qa1", "qa2", "qa3"
  env = "qa1"

  # Expected values are "Chrome", "Chrome-Headless", "Firefox", "IE", "Edge", "Safari"
  browser = "Chrome-Headless"

  # Expected values are "local" or "saucelabs"
  run.at = "local"

  # Deploy "prod.latest" default
  deploy = "true"

  # Resets the Database
  resetDB = "true"

  noAttempts = "1"

  loadFixtures = "no"

  # Expected values are "@investor-regression", "@login",
  cucumber.options = ""

  # Expected values are the build versions
  buildWebApp = "prod.latest"
  buildAdminWebApp = "prod.latest"
  buildUserAPI = "prod.latest"
  buildCustomerAPI = "prod.latest"
  buildInvestorAPI = "prod.latest"

  mySQLdbURL = "jdbc:mysql://db-url.com"

  # Expected value is "1" for API logs
  debug = "1"
  apiRequest.timeout = "10000"

  #environment values: local, saucelabs
  #TASK saucelabs = enter capabilities as parameters
  #platforms = Windows 10, Windows 8.1, Windows XP, OSX 11, OSX 10 / for Appium: Android 5.1, Android 5.0, Android 4.4
  #----default values START-----
  sauceplatform = "Windows 10"
  saucebrowser = "Chrome"
  saucebrowserversion = "55"

  sauceLabs.username = "username"
  sauceLabs.password = "token"
  sauceLabs.url = "https://url:443/wd/hub"
  sauceLabs.androiddevicename = "Samsung Galaxy S4 Emulator"
  #testscenarios=invest
  #debugmode=off

  jenkinsjobrunenv = "env"
  jenkins.username = "username"
  jenkins.password = "token"
  segmentWriteKey = "someKey"
  automationReports.bucketName = "bucket-name"
  aws.accessKey.id = "ID"
  aws.secret.accessKey = "KEY"

  # Passwords are encoded
  env1 {
    superUserEmail = "email1@lendinvest.com"
    superUserPassword = "password1=="
    userPassword = "password2=="
    csmAdminPassword = "password3=="
    serviceWorker = "password4"
    qa1_db.password = "password"
  }

  env2 {
    superUserEmail = "email1@lendinvest.com"
    superUserPassword = "password1=="
    userPassword = "password2=="
    csmAdminPassword = "password3=="
    serviceWorker = "password4"
    qa1_db.password = "password"
  }

  env3 {
    superUserEmail = "email1@lendinvest.com"
    superUserPassword = "password1=="
    userPassword = "password2=="
    csmAdminPassword = "password3=="
    serviceWorker = "password4"
    qa1_db.password = "password"
  }
}